import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    allWhite: {
      main: '#fff',
      light: '#fff',
      constrastText: '#fff',
    },
    textBlack: {
      main: '#000',
      contrastText: '#000',
    }
  }
})
